1. = 和 == 有什么区别？
2. “状态码正确并且响应时间合格”应该使用 and 还是 or？
3. “用户名为空或者密码为空”应该使用 and 还是 or？
4. not True 的结果是什么？
5. None 和字符串 "None" 是否相同？

1. =是赋值，==表示判断两个变量是否相等
2. 应该使用and
3. 应该使用or
4. False
5. 不相同，字符串是字符串，None表示没有值


1. in 用于列表时检查什么？
2. in 用于字符串时检查什么？
3. 为什么 "test" in "testing" 是 True？
4. not in 与对 in 取反是否一致？
5. 为什么有效环境条件使用 and？

1. 用于检查列表中的元素是否包含对应的内容
2. 用于检查字符串中是否包含一样的片段
3. 因为testing的前四个字母就是test，能匹配上
4. 是一致的
5. 环境必须在允许列表中,同时还必须不在禁止列表中,所以必须用and