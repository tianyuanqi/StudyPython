1. = 和 == 有什么区别？
2. “状态码正确并且响应时间合格”应该使用 and 还是 or？
3. “用户名为空或者密码为空”应该使用 and 还是 or？
4. not True 的结果是什么？
5. None 和字符串 "None" 是否相同？

1. =是复制，==表示判断两个变量是否相等
2. 应该使用and
3. 应该使用or
4. false
5. 不相同，字符串是字符串，None表示没有值